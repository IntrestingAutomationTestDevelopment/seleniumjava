/*
 * package tests;
 * 
 * import static org.testng.Assert.assertEquals;
 * 
 * import org.testng.annotations.Test;
 * 
 * import Utilities.Base; import io.restassured.RestAssured; import
 * io.restassured.response.Response; //import org.junit.jupiter.api.Test;
 * //import static org.junit.jupiter.api.Assertions.assertEquals;
 * 
 * public class GetRequestTesting extends Base{
 * 
 * 
 * @Test public void testGetRequest() { // Define the API URL String url =
 * "https://jsonplaceholder.typicode.com/posts/1";
 * 
 * // Send GET request Response response = RestAssured.get(url);
 * 
 * // Assert status code is 200 assertEquals(200, response.statusCode(),
 * "Expected status code 200");
 * 
 * // Assert that 'userId' exists in the response body assertEquals(1,
 * response.jsonPath().getInt("userId"), "Expected userId to be 1");
 * assertEquals(1, response.jsonPath().getInt("id"),
 * "Expected post ID to be 1");
 * 
 * System.out.println("GET Request Test Passed!"); } }
 */
package testksrabhilash;

