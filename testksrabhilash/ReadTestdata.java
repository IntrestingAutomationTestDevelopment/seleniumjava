//import testksrabhilash;
//import java.util.List;

//public class ReadTestdata {
	/*
	 * public static void main(String[] args) { // Path to the Excel file String
	 * filePath = "path/to/testdata.xlsx";
	 * 
	 * // Sheet name in the Excel file String sheetName = "Sheet1";
	 * 
	 * // Read test data List<TestData> testDataList =
	 * ExcelReader.readTestData(filePath, sheetName);
	 * 
	 * // Use test data in your tests for (TestData data : testDataList) {
	 * System.out.println("Username: " + data.getUsername());
	 * System.out.println("Password: " + data.getPassword()); } }
	 */
//}
